<match fwd.raw.monit>
  type rewrite_tag_filter

  rewriterule1 hostname (.+) tmp.monit.$1
</match>

<match tmp.monit.*>
  type forest
  subtype grep

  remove_prefix tmp.monit

  <template>
    input_key message
    regexp (error|ERROR)

    add_tag_prefix notification
  </template>
</match>
